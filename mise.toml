# Tool versions managed by mise (https://mise.jdx.dev/).
# Run `mise install` once from the repo root to get everything in scope.
[tools]
node = "lts"
hugo = "0.160.1" # see .github/workflows/deploy.yml
uv   = "latest"
